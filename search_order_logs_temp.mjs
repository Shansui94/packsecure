import dotenv from "dotenv"; import { createClient } from "@supabase/supabase-js"; dotenv.config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.VITE_SUPABASE_SERVICE_ROLE_KEY); async function run() { const { data: logs, error } = await supabase.from("user_activity_logs").select("*").order("created_at", { ascending: false }).limit(200); if (error) { console.error(error); return; } console.log("--- SEARCHING FOR TAUFIK IN LOGS ---"); logs.forEach(l => { const detailsStr = JSON.stringify(l.details); if (detailsStr.includes("DO-Taufik-260613") || detailsStr.includes("Taufik") || l.email.includes("taufik")) { console.log("User: " + l.email + " | Action: " + l.action + " | Details: " + detailsStr + " | CreatedAt: " + l.created_at); } }); } run();

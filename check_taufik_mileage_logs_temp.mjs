import dotenv from "dotenv"; import { createClient } from "@supabase/supabase-js"; dotenv.config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.VITE_SUPABASE_SERVICE_ROLE_KEY); async function run() { const taufikId = "33309909-7102-41da-adc7-44606fdcb09a"; const { data: logs, error } = await supabase.from("lorry_mileage_logs").select("*").eq("driver_id", taufikId).order("created_at", { ascending: false }).limit(5); if (error) { console.error(error); return; } console.log("--- TAUFIK MILEAGE LOGS ---"); logs.forEach(l => { console.log("Lorry: " + l.lorry_id + " | Mileage: " + l.mileage + " | Type: " + l.log_type + " | CreatedAt: " + l.created_at); }); } run();

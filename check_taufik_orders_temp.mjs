import dotenv from "dotenv"; import { createClient } from "@supabase/supabase-js"; dotenv.config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.VITE_SUPABASE_SERVICE_ROLE_KEY); async function run() { const taufikId = "33309909-7102-41da-adc7-44606fdcb09a"; const { data: orders, error } = await supabase.from("sales_orders").select("*").eq("driver_id", taufikId).order("created_at", { ascending: false }).limit(10); if (error) { console.error(error); return; } console.log("--- TAUFIK LATEST 10 ORDERS ---"); orders.forEach(o => { console.log("SO: " + o.order_number + " | Status: " + o.status + " | POD: " + o.pod_photo_url + " | CreatedAt: " + o.created_at + " | Notes: " + o.notes); }); } run();

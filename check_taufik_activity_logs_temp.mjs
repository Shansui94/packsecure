import dotenv from "dotenv"; import { createClient } from "@supabase/supabase-js"; dotenv.config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.VITE_SUPABASE_SERVICE_ROLE_KEY); async function run() { const taufikEmail = "taufik.8646@packsecure.com"; const { data: logs, error } = await supabase.from("user_activity_logs").select("*").eq("email", taufikEmail).order("created_at", { ascending: false }).limit(50); if (error) { console.error(error); return; } console.log("--- TAUFIK LATEST ACTIVITY LOGS ---"); logs.forEach(l => { console.log("Action: " + l.action + " | Details: " + JSON.stringify(l.details) + " | CreatedAt: " + l.created_at); }); } run();
